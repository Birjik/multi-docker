language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t birjik/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true birjik/react-test npm test -- --coverage

after_success:
  - docker build -t birjik/multi-client ./client
  - docker build -t birjik/multi-nginx ./client
  - docker build -t birjik/multi-server ./client
  - docker build -t birjik/multi-worker ./client
  
